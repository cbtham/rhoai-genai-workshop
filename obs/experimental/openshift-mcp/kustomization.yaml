apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - openshift-deployment.yaml
  - openshift-rolebinding.yaml
  - openshift-service.yaml
  - openshift-serviceaccount.yaml

# Namespace is not specified here - will use the namespace from -n flag
# Deploy with: oc apply -k obs/experimental/openshift-mcp -n <your-namespace>